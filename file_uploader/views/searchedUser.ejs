<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css">
  </head>
<body>
  <header>
    <%- include("partials/errors.ejs") %>
    <% if(user.role === 'USER') { %>
      <%- include('partials/navbar2.ejs', {links: memberAuthenticatedLinks}) %>
    <% } else if (user.role === 'GUEST') { %>
      <%- include('partials/navbar2.ejs', {links: guestAuthenticatedLinks}) %>
    <% } else if (user.role === 'ADMIN') { %>
      <%- include('partials/navbar2.ejs', {links: adminAuthenticatedLinks}) %>
    <% } %> 
  </header>
  <h2 class="page-header">⤴️⤵️ File Uploader - Search User Result</h2>
  <h3 class="user-result-header">Match users:</h3>
  <main class="searched-users">
    <% if(users.length === 0) { %>
      <p class="no-searched-users">No users matched!</p>
    <% } else { %>
      <% users.forEach((user) => { %>
        <%- include('partials/user_entry.ejs', {user: user, isAdmin: isAdmin}) %>
      <% }); %>
    <% } %>
  </main>
</body>
</html>
