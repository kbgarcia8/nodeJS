<!-- index.ejs -->
<html>
  <head>
    <link rel="stylesheet" href="/styles.css">
  </head>
  
  <body>
    <%- include('navbar', {links: links}) %>
    <ul>
      <% users.forEach((user) => { %>
        <%- include('users/user', {user: user}); %>
        <!-- Take note that include was done per user since -->
        <!-- user.ejs contains li element -->
      <% }); %>
    </ul>
  </body>
</html>
